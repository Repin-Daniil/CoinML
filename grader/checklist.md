# ✅ Чеклист перед запуском обучения

## 📋 Подготовка окружения

- [ ] Python 3.8+ установлен
  ```bash
  python --version
  ```

- [ ] Создано виртуальное окружение
  ```bash
  python -m venv venv
  source venv/bin/activate  # Linux/Mac
  # или venv\Scripts\activate  # Windows
  ```

- [ ] Установлены все зависимости
  ```bash
  pip install -r requirements.txt
  ```

- [ ] GPU доступен (опционально, но рекомендуется)
  ```bash
  python -c "import tensorflow as tf; print(tf.config.list_physical_devices('GPU'))"
  ```

---

## 📁 Подготовка данных

- [ ] Датасет размещен в правильной структуре
  ```
  dataset/
  ├── 1/
  │   ├── coin001_obverse.jpg
  │   ├── coin001_reverse.jpg
  │   └── ...
  ├── 2/
  ├── 3/
  ├── 4/
  └── 5/
  ```

- [ ] Проверено наличие обеих сторон для каждой монеты
  ```bash
  # Должно быть четное число файлов в каждой папке
  ls -1 dataset/1/*.jpg | wc -l
  ```

- [ ] Проверено, что имена файлов соответствуют формату
  - `{coin_id}_obverse.jpg`
  - `{coin_id}_reverse.jpg`

- [ ] Проведена визуализация данных
  ```bash
  python visualize_data.py
  # Проверьте визуализации в папке visualizations/
  ```

---

## ⚙️ Конфигурация

- [ ] Файл `config.yaml` существует и корректен
  ```bash
  cat config.yaml
  ```

- [ ] Путь к датасету указан правильно
  ```yaml
  data:
    path: "./dataset/"  # ← проверьте этот путь
  ```

- [ ] Размер изображения соответствует вашим данным
  ```yaml
  data:
    image_size: 384  # стандартное значение
  ```

- [ ] Batch size подходит для вашего GPU/CPU
  ```yaml
  training:
    batch_size: 32  # уменьшите до 8-16 если мало памяти
  ```

- [ ] Learning rate и другие параметры разумны
  ```yaml
  training:
    learning_rate: 0.001  # стандартный старт
  ```

---

## 🔬 WandB настройка

- [ ] Аккаунт WandB создан на https://wandb.ai

- [ ] WandB API ключ получен

- [ ] Выполнен вход в WandB
  ```bash
  wandb login
  ```

- [ ] Проект и run name настроены в config.yaml
  ```yaml
  wandb:
    project_name: "CoinGrader-Project"
    run_name: "experiment_1"  # уникальное имя для каждого эксперимента
  ```

---

## 🚀 Первый запуск

- [ ] Создана папка для сохранения моделей
  ```bash
  mkdir -p saved_models
  ```

- [ ] Создана папка для логов
  ```bash
  mkdir -p logs
  ```

- [ ] Выполнен тестовый запуск на небольшом количестве эпох
  ```yaml
  # В config.yaml временно установите
  training:
    epochs: 3  # для проверки работоспособности
  ```

- [ ] Проверен вывод в консоли
  - Датасет загружен корректно
  - Модель создана успешно
  - Обучение началось

- [ ] Проверен WandB dashboard
  - Метрики логируются
  - Графики отображаются

---

## 🐛 Частые проблемы

### Ошибка: "No module named 'tensorflow'"
```bash
pip install tensorflow
```

### Ошибка: "CUDA out of memory"
Уменьшите batch_size в config.yaml:
```yaml
training:
  batch_size: 8  # или даже 4
```

### Ошибка: "Dataset path not found"
Проверьте путь в config.yaml:
```yaml
data:
  path: "./dataset/"  # убедитесь что папка существует
```

### Ошибка: "No samples found"
Проверьте структуру папок и формат имен файлов

### WandB не логирует
```bash
# Повторно выполните вход
wandb login --relogin
```

---

## 📊 После первого запуска

- [ ] Проверены метрики обучения
  - Train accuracy растет?
  - Val accuracy растет?
  - Есть ли переобучение (train >> val)?

- [ ] Проверен confusion matrix в WandB

- [ ] Сохранена лучшая модель
  ```bash
  ls -lh saved_models/
  ```

- [ ] Созданы логи
  ```bash
  ls -lh logs/
  ```

---

## 🎯 Готовность к production обучению

После успешного тестового запуска:

- [ ] Увеличен epochs в config.yaml
  ```yaml
  training:
    epochs: 50  # или больше
  ```

- [ ] Настроен оптимальный batch_size для вашего GPU

- [ ] Выбрана подходящая архитектура
  - SimpleCNN - для быстрых экспериментов
  - MobileNetV2 - для баланса скорость/качество
  - ResNet50 - для максимального качества

- [ ] Настроена аугментация под ваши данные

- [ ] Подготовлено несколько конфигов для экспериментов

- [ ] Создан план экспериментов (см. EXAMPLES.md)

---

## 🔍 Мониторинг во время обучения

Во время обучения следите за:

- [ ] **Loss снижается** (и train, и val)
- [ ] **Accuracy растет** (и train, и val)
- [ ] **Нет большого разрыва** между train и val (признак переобучения)
- [ ] **Learning rate снижается** по расписанию
- [ ] **GPU утилизация** высокая (если используете GPU)

Команда для мониторинга GPU:
```bash
watch -n 1 nvidia-smi
```

---

## ✅ Финальная проверка

Перед длительным обучением убедитесь:

- [ ] Все данные на месте
- [ ] Конфигурация проверена
- [ ] Тестовый запуск прошел успешно
- [ ] WandB логирует корректно
- [ ] Достаточно места на диске (для моделей и логов)
- [ ] Достаточно GPU памяти (если используете GPU)

---

## 🎉 Готово к запуску!

Если все пункты отмечены, можно начинать полноценное обучение:

```bash
# Финальная команда запуска
python train.py --config config.yaml

# Или с несколькими экспериментами
python train.py --config experiments/config_resnet50.yaml
python train.py --config experiments/config_mobilenet.yaml
python train.py --config experiments/config_simple.yaml
```

**Удачи! 🚀**