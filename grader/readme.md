# 🪙 Coin Grader - CNN для классификации сохранности монет

Проект для обучения сверточной нейронной сети, классифицирующей монеты по 5 классам сохранности на основе изображений аверса и реверса.

## 🎯 Ключевые особенности

- **Двухвходовая архитектура**: Модель принимает аверс и реверс монеты
- **Shared Weights**: Общий бэкбон для обеих сторон монеты
- **Гибкая конфигурация**: Все гиперпараметры в `config.yaml`
- **WandB Integration**: Полное логирование экспериментов
- **Production-ready**: Готово к развертыванию на облачных GPU

## 📂 Структура проекта

```
grader/
├── config.yaml          # Конфигурация гиперпараметров
├── data_loader.py       # Загрузка и предобработка данных
├── model.py             # Архитектура нейросети
├── train.py             # Главный скрипт обучения
├── predict.py           # Предсказание для одной монеты
├── evaluate.py          # Оценка модели на тестовой выборке
├── visualize_data.py    # Визуализация и анализ датасета
├── utils.py             # Вспомогательные функции
├── requirements.txt     # Зависимости
├── quickstart.sh        # Скрипт быстрой установки
├── EXAMPLES.md          # Примеры использования
├── PROJECT_SUMMARY.md   # Краткое описание
└── README.md           # Документация
```

## 📊 Структура датасета

```
dataset/
├── 1/  (Плохое состояние)
│   ├── coin001_obverse.jpg
│   ├── coin001_reverse.jpg
│   ├── coin002_obverse.jpg
│   └── coin002_reverse.jpg
├── 2/
├── 3/
├── 4/
└── 5/  (Идеальное состояние)
```

## 🚀 Быстрый старт

### Автоматическая установка (рекомендуется)

```bash
chmod +x quickstart.sh
./quickstart.sh
```

Скрипт автоматически:
- Создаст виртуальное окружение
- Установит все зависимости
- Проверит наличие GPU
- Настроит структуру проекта
- Предложит настроить WandB

### Ручная установка

### 1. Установка зависимостей

```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

pip install -r requirements.txt
```

### 2. Настройка WandB

```bash
wandb login
```

### 3. Подготовка данных

Разместите датасет в папке `./dataset/` согласно структуре выше.

### 4. Запуск обучения

**Базовый запуск:**
```bash
python train.py
```

**С кастомной конфигурацией:**
```bash
python train.py --config my_config.yaml
```

**С предобученными весами ImageNet:**
```bash
python train.py --pretrained
```

### 5. Предсказание на новых данных

**Для одной монеты:**
```bash
python predict.py \
    --model saved_models/best_model.h5 \
    --obverse path/to/coin_obverse.jpg \
    --reverse path/to/coin_reverse.jpg
```

Пример вывода:
```
==================================================
РЕЗУЛЬТАТЫ
==================================================

Предсказанный класс: 4
Уверенность: 87.23%

Вероятности по классам:
  Класс 1: 0.05% 
  Класс 2: 2.41% ██
  Класс 3: 8.12% ████
  Класс 4: 87.23% ██████████████████████████████
  Класс 5: 2.19% ██
```

### 6. Оценка модели

**Полная оценка на валидационной выборке:**
```bash
python evaluate.py --model saved_models/best_model.h5
```

Создаст:
- `evaluation_results/confusion_matrix.png` - визуализация
- `evaluation_results/evaluation_results.txt` - детальный отчет
- Classification Report с Precision/Recall/F1-score по классам

### 7. Визуализация датасета

**Анализ и визуализация данных перед обучением:**
```bash
python visualize_data.py
```

Создаст:
- `visualizations/class_distribution.png` - распределение классов
- `visualizations/dataset_samples.png` - примеры изображений
- Вывод статистики размеров изображений в консоль

## ⚙️ Конфигурация

Основные параметры в `config.yaml`:

### Параметры данных
- `image_size`: Размер изображения (384x384)
- `num_classes`: Количество классов (5)
- `val_split`: Доля валидационной выборки (0.2)

### Параметры обучения
- `batch_size`: Размер батча (32 для A100)
- `epochs`: Количество эпох (50)
- `learning_rate`: Начальный LR (0.001)
- `optimizer`: Оптимизатор (Adam/RMSprop/SGD)

### Архитектура модели
- `backbone_name`: Бэкбон (`ResNet50`, `MobileNetV2`, `SimpleCNN`)
- `dropout_rate`: Dropout (0.5)
- `dense_units`: Размер Dense слоя (256)

### Аугментация
- `flip_mode`: Отражения (horizontal/vertical/both)
- `rotation_factor`: Поворот (0.1 = 36°)
- `brightness_factor`: Яркость (0.2)
- `zoom_factor`: Масштабирование (0.1)

## 🏗️ Архитектура модели

```
Input A (Obverse)  ──┐
                     ├──> Shared Backbone ──> GlobalAvgPool ──┐
Input B (Reverse)  ──┘                                         │
                                                               ├──> Concatenate
                                                               │
                                                               └──> Dense(256) 
                                                                    ──> Dropout
                                                                    ──> Dense(128)
                                                                    ──> Dropout
                                                                    ──> Dense(5, softmax)
```

## 📈 Мониторинг обучения

Все метрики логируются в WandB:
- Train/Val Loss & Accuracy
- Learning Rate изменения
- Confusion Matrix (при использовании WandbCallback)
- Примеры предсказаний

## 💾 Сохранение моделей

Модели сохраняются в `./saved_models/`:
- `{run_name}_best.h5` - лучшая модель по val_accuracy
- `{run_name}_final.h5` - финальная модель после обучения

## 🔧 Callbacks

1. **WandbCallback** - логирование в W&B
2. **ModelCheckpoint** - сохранение лучшей модели
3. **ReduceLROnPlateau** - динамическое снижение LR
4. **EarlyStopping** - остановка при переобучении
5. **TensorBoard** - дополнительная визуализация

## 🎛️ Эксперименты

### Попробуйте разные бэкбоны:

```yaml
# config.yaml
model:
  backbone_name: "ResNet50"      # Глубокая архитектура
  # backbone_name: "MobileNetV2"  # Легковесная версия
  # backbone_name: "SimpleCNN"    # С нуля для обучения
```

### Настройка аугментации:

```yaml
augmentation:
  flip_mode: "horizontal_and_vertical"
  rotation_factor: 0.15  # Увеличить поворот
  brightness_factor: 0.3  # Сильнее менять яркость
  zoom_factor: 0.15
```

## 📝 Логирование

Все эксперименты логируются:
- **WandB**: Онлайн дашборд с графиками
- **TensorBoard**: Локальная визуализация (`./logs/`)
- **Console**: Прогресс и метрики в терминале

## 🐛 Решение проблем

### GPU не обнаружен
```bash
# Проверьте установку CUDA
nvidia-smi

# Проверьте TensorFlow
python -c "import tensorflow as tf; print(tf.config.list_physical_devices('GPU'))"
```

### Ошибки памяти
Уменьшите `batch_size` в config.yaml:
```yaml
training:
  batch_size: 16  # Вместо 32
```

### Датасет не найден
Проверьте путь в config.yaml:
```yaml
data:
  path: "./dataset/"  # Убедитесь что папка существует
```

## 🔬 Расширение функционала

### Добавление фильтрации по БД

В `data_loader.py` найдите секцию:

```python
# ============================================
# МЕСТО ДЛЯ ФИЛЬТРАЦИИ (проверка в БД и т.д.)
# ============================================
# Пример:
# if not should_include_coin(coin_id):
#     continue
# ============================================
```

### Добавление нового бэкбона

В `model.py` добавьте в `create_backbone()`:

```python
elif backbone_name == "YourModel":
    backbone = keras.applications.YourModel(
        weights=None,
        include_top=False,
        input_shape=input_shape
    )
```

## 📊 Метрики качества

Модель оптимизируется по:
- **Accuracy**: Общая точность классификации
- **Loss**: Sparse Categorical Crossentropy

Дополнительные метрики в WandB:
- Confusion Matrix
- Per-class Precision/Recall
- Learning curves

## 🎓 Рекомендации по обучению

1. **Начните с малого**: `SimpleCNN` на небольшом датасете
2. **Экспериментируйте с LR**: 0.001 → 0.0001 → 0.00001
3. **Используйте аугментацию**: Увеличивает обобщающую способность
4. **Мониторьте переобучение**: val_loss должен снижаться
5. **Сохраняйте чекпоинты**: Лучшая модель может быть не в конце

## 🤝 Поддержка

Для вопросов и багов создавайте issue в репозитории.

## 📄 Лицензия

MIT License

---

**Удачи в обучении! 🚀**